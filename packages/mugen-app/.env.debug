# `vite build --mode debug` (npm run build:debug) — the debug APK.
# Builds the debug tools in: the battle preview and the title's DEBUG chip.
# A release build (`npm run build`) does not read this file.
VITE_MUGEN_DEBUG_TOOLS=1
